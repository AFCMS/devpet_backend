{
  "description": "DevPet Configuration",
  "type": "object",
  "properties": {
    "github_token": {
      "description": "GitHub token to access the GitHub API\nMust have the `read:user` scope, overwise private profile data will not be accessible",
      "type": "string"
    },
    "serial_port": {
      "description": "The path to the serial port to use",
      "type": "string",
      "examples": [
        "/dev/rfcomm0"
      ]
    }
  },
  "required": [
    "github_token",
    "serial_port"
  ],
  "additionalItems": false
}